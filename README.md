# ypsi-winter-games
Ypsilanti Winter Games 2026

## Running Locally
1. Clone the repo
2. Install dependencies: `npm install`
3. Set up environment variables (in `.env.local` file -> taking template from `env.example`)
4. Run development server: `npm run dev`
